// ============================================================
// PaySentinelIQ — Auth Layout
// Passes children directly — the login page handles layout + background
// ============================================================

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
